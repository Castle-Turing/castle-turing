# modules/ — shared framework modules

NixOS and home-manager modules that make up the castle: window manager glue,
mail machinery, journal tooling, escalation channels. Hardware-agnostic and
person-agnostic by construction: anything personal is consumed from the
private layer through a documented interface, never defined here.
